# XCanvas

XCanvas is a light-weight C++ library for C++20 that provides a graphics API heavily inspired by the
HTML5 `<canvas>` WebGL API.

XCanvas provides the following features:

- `Application` class for creating native desktop windows to draw within
- `Canvas` class for drawing

## TODO

Write an actual readme.